import 'package:flutter/material.dart';

class BuscaCnpjSubmitButton extends StatelessWidget {
  const BuscaCnpjSubmitButton({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const ElevatedButton(onPressed: null, child: Text("Buscar"));
  }
}
